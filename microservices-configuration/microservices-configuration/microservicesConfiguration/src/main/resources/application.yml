app:
 name: My app
 description: Welcome to ${app.name}

my:
 greeting: Hello World
 list: 
   values: one, two, three

server.port: 8080
  
management.endpoints.web.exposure.include: "*"

spring.cloud.config.uri: http://localhost:8888

spring:
  application:
    name: microservicesConfiguration